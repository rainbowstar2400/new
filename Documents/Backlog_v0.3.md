# Backlog v0.3

- 最終更新: 2026-02-15（v0.3再編実装計画反映）
- 参照: `PRD_v0.3.md`, `UseCases_v0.3.md`, `Backlog_v0.2.md`, `Backlog_v0.1.md`
- 目的: v0.3再編（UI刷新 + 分類境界 + 一覧調整）の実装タスク定義

## 1. 運用ルール

- `v0.2` は凍結し、仕様更新は `v0.3` のみで行う。
- 優先順は `P1-01 -> P2-01 -> P2-02`。
- 仕様変更時は `Documents/*_v0.3.md` と `Issues/*_v0.3.md` を同時更新する。
- 実装は公開API/DB変更なしで進める。

## 2. v0.3 実装バックログ

| ID | 種別 | タイトル | 主な内容 | 依存 | 受け入れ条件 |
| --- | --- | --- | --- | --- | --- |
| P1-01 | UI/UX | モバイル下部タブUI全面刷新 | 3タブ（チャット/メモ・タスク/設定）、下部固定ナビ、`safe-area` 対応、タブ/フィルタ復元、`?taskId=` 深リンク遷移 | なし | UC-30〜UC-35, FR-26〜FR-32 |
| P2-01 | Logic | 分類境界ケース拡張 | `〜たい` の `memo(want)` 強化、単語タスク判定強化、曖昧時確認徹底、`task_or_memo -> memo_category` 強制、未分類保存防止 | P1-01（UIで確認可能にするため） | UC-36〜UC-40, FR-33〜FR-37 |
| P2-02 | UX | 一覧画面微調整 | `!期日設定` バッジ再設計、メタ情報優先度整理、フィルタ/強調表示統一、空状態UI | P1-01 | UC-41, FR-38〜FR-40 |

## 3. テストタスク（各IDに内包）

### P1-01

- web単体: タブ復元、フィルタ復元、`?taskId=` 遷移
- web E2E: 下部タブ遷移、3状態フィルタ、モバイルビューポート操作

### P2-01

- shared単体: `〜たい`, 単語タスク, アイデア語, 曖昧入力
- api結合: `task_or_memo -> memo_category` 強制、未確定保存禁止

### P2-02

- web単体/E2E: `!期日設定` 表示、空状態表示、一覧視認性回帰

## 4. v0.1 回帰チェック（必須）

- `明日18時に洗濯` はタイトル `洗濯` で保存される
- `京都にいきたい` は `memo(want)` で保存される
- `転職準備` は確認前に保存されない
- `メモ` 選択後は `memo_category` 確定前に保存されない

## 5. 完了条件

- `P1-01`, `P2-01`, `P2-02` が実装・テスト済み
- `pnpm test` と `pnpm --filter @new/web test:e2e` が通過
- `Documents/*_v0.3.md` と `Issues/*_v0.3.md` が同期している

